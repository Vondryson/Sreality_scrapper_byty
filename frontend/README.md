# Frontend

Cílová Next.js/TypeScript aplikace bude inicializována v tasku `M3-01` po stabilizaci API kontraktu. Tento adresář nyní pouze vymezuje vlastnictví frontendového kódu.

Po inicializaci musí obsahovat vlastní lockfile, strict TypeScript, lint, testy a příkazy popsané v kořenovém README.

